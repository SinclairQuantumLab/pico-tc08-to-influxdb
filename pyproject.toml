[project]
name = "pico-tc08-to-influxdb"
version = "0.1.0"
description = "Relay PicoLog TC-08 thermocouple readings to InfluxDB."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "influxdb-client>=1.50.0",
    "picosdk>=1.1",
]
